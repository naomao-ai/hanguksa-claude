rules_version = '2';

// 모든 클라이언트 직접 접근을 차단한다.
// 데이터는 서버(Admin SDK)를 통해서만 읽고 쓰며, Admin SDK는 규칙을 우회한다.
// 따라서 앱의 API 라우트는 정상 동작하고, 외부에서의 직접 접근은 막힌다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
